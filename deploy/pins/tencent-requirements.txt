# Vendored, unmodified copy of Tencent's requirements.txt for HunyuanImage-3.0.
#
# Source:   https://github.com/Tencent-Hunyuan/HunyuanImage-3.0
# Path:     requirements.txt
# Commit:   6e9113a692a27a0751d82aba3b2015a876646c03 (2026-06-23)
# Fetched:  2026-08-23
#
# This is the authoritative baseline per the environment policy in
# deploy/pins/torch-cuda-notes.md: Tencent pins > ComfyUI needs >
# node-pack needs > convenience. Do not edit this file by hand — if a
# newer Tencent commit needs to be picked up, re-fetch it wholesale and
# update the commit SHA above. Deviations required to make the node pack
# work are recorded separately in deploy/pins/DEVIATIONS.md, not by
# editing this file.

# Core dependencies
einops==0.8.1
numpy==2.2.0
pillow==12.0.0
diffusers==0.35.2
safetensors==0.7.0
tokenizers==0.22.0
transformers[accelerate,tiktoken]==4.57.1
huggingface_hub[cli]
loguru>=0.7.3

# PyTorch with CUDA 12.8 support (install separately)
# torch==2.8.0
# torchvision==0.23.0
# torchaudio==2.8.0
# Install with: pip install torch==2.8.0 torchvision==0.23.0 torchaudio==2.8.0 --index-url https://download.pytorch.org/whl/cu128

# Performance optimizations (recommended for up to 3x faster inference)
# flashinfer-python==0.5.0
# Install with: pip install flashinfer-python==0.5.0

# Interactive demo
gradio>=4.21.0
